fastapi>=0.110
uvicorn[standard]>=0.29
gunicorn>=21        # 운영 구동 — uvicorn 워커 + --max-requests 로 워커 주기 재활용(메모리 회수)
pydantic>=2.6
supabase>=2.8
realtime>=2.0
websockets>=13      # realtime 2.x 가 websockets.asyncio 를 사용하므로 하한 고정
python-dotenv>=1.0
httpx>=0.27
openai>=1.40
